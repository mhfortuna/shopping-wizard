/*  
==========================================
changing prices
==========================================
*/
const colorThumbails = document
  .querySelectorAll(".color-thumbnails-wrapper img");

  colorThumbails.forEach(element => {
    element.addEventListener("click", fnColorThumbails)    
  });

function fnColorThumbails(event) {
    
}
